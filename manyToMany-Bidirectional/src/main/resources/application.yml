#http://localhost:8381/swagger-ui/index.html
spring:
  application:
    name: many-to-many-bidirectional
  datasource:
    url: jdbc:postgresql://localhost:5432/many-to-many
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8381